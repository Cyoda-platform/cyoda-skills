{
  "skill_name": "migrate",
  "evals": [
    {
      "id": 1,
      "prompt": "/cyoda:migrate",
      "expected_output": "Verifies local instance is running, exports all entity models/workflows AND JSON schemas to migration/ directory, invokes setup+login for cloud, imports each workflow, runs /cyoda:test against cloud, instructs user to update app config",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "verifies-local", "text": "Verifies local cyoda-go instance is running before doing anything", "type": "behavior" },
        { "id": "exports-workflows", "text": "Exports all entity workflows to migration/ directory", "type": "behavior" },
        { "id": "exports-schemas", "text": "Exports JSON Schema (field definitions) for each entity via /api/model/export/JSON_SCHEMA/{entity}/{version}", "type": "behavior" },
        { "id": "confirms-export-complete", "text": "Confirms both schema and workflow files exist for each entity before proceeding", "type": "behavior" },
        { "id": "invokes-setup-login", "text": "Invokes /cyoda:setup (cloud mode) then /cyoda:login", "type": "behavior" },
        { "id": "imports-to-cloud", "text": "Imports each workflow to cloud", "type": "behavior" },
        { "id": "runs-test-on-cloud", "text": "Runs /cyoda:test against cloud after import", "type": "behavior" },
        { "id": "instructs-config-update", "text": "Instructs user to update app config to point to cloud", "type": "behavior" },
        { "id": "no-migrate-without-verify", "text": "Does NOT attempt export without first verifying local instance", "type": "behavior" },
        { "id": "no-skip-schema-export", "text": "Does NOT skip schema export — only exporting workflow is insufficient", "type": "behavior" }
      ]
    },
    {
      "id": 2,
      "prompt": "/cyoda:migrate",
      "expected_output": "Detects local instance is unreachable, tells user to start cyoda-go first, stops gracefully without attempting export",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "detects-unreachable", "text": "Detects local instance is unreachable", "type": "behavior" },
        { "id": "instructs-start", "text": "Tells user to start cyoda-go first", "type": "behavior" },
        { "id": "stops-gracefully", "text": "Stops gracefully — does not attempt export", "type": "behavior" },
        { "id": "no-silent-proceed", "text": "Does NOT silently proceed to cloud setup without local data", "type": "behavior" }
      ]
    },
    {
      "id": 3,
      "prompt": "/cyoda:migrate — my order entity already exists in cloud with an old workflow",
      "expected_output": "Checks what exists in cloud before importing, shows the conflict, asks whether to overwrite or skip, proceeds based on user choice",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "checks-cloud-first", "text": "Checks what already exists in cloud before importing", "type": "behavior" },
        { "id": "shows-conflict", "text": "Shows the conflict to the user", "type": "format" },
        { "id": "asks-overwrite-or-skip", "text": "Asks whether to overwrite or skip the conflicting workflow", "type": "behavior" },
        { "id": "respects-user-choice", "text": "Proceeds based on user's choice", "type": "behavior" },
        { "id": "no-silent-overwrite", "text": "Does NOT silently overwrite existing cloud workflows", "type": "behavior" }
      ]
    }
  ]
}
